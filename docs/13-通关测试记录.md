# 真实通关测试记录

## 百图当前结论（2026-08-11）

在本地 HTTP 服务 `http://127.0.0.1:4174/` 上，战士完成 1/1 次 100 图快进路线。测试按顺序完成 100 张地图、698 只必需普通怪、100 名区域首领、99 次前进出口切换，并在每关后自动保存、读取存档、真实刷新页面和恢复当前进度。

最终输出：

```text
CHECKPOINT 100/100 stage_100 Lv.41 0/100 SAVED+RESTORED
RUN 1/1 warrior Lv.41: PASS
ALL 1/1 JOURNEYS PASSED
```

100 个检查点全部通过，最终界面为 `百图征途已通关 / 全部完成`。测试过程中没有出现卡图、错误出口、升级边界错误、刷新后回退或存档丢失。

### 成长边界实测

| 检查点 | 实际状态 | 结论 |
|---:|---:|---|
| 第 1 关 | Lv.2 · 0/100 | 前期每图一级生效 |
| 第 10 关 | Lv.11 · 0/100 | 第一阶段完成 |
| 第 11 关 | Lv.11 · 50/100 | 两图一级开始 |
| 第 30 关 | Lv.21 · 0/100 | 第二阶段完成 |
| 第 31/32/33 关 | Lv.21 · 33/66，Lv.22 · 0 | 三图一级循环正确 |
| 第 60 关 | Lv.31 · 0/100 | 第三阶段完成 |
| 第 61/62/63/64 关 | Lv.31 · 25/50/75，Lv.32 · 0 | 四图一级循环正确 |
| 第 100 关 | Lv.41 · 0/100 | 最终成长正确 |

### 快进方式与证据强度

测试使用 `?e2e=1&fast=1`。`fast=1` 仅把攻击伤害放大 25 倍，页面时钟由 Playwright 快进；驱动仍通过 Canvas 点击、方向键、`J/1/2/3/4/R/F/Q/T` 完成战斗和移动。只读测试接口暴露地图目录与快照，不提供写状态方法。

因此本轮能证明：100 图目录可达、普通怪/Boss 双条件生效、成长分段正确、出口顺序正确、每关自动保存并可刷新恢复、最终结算正确。它不能证明正式速度下的总耗时、三职业后期平衡、药水经济和装备更换率已经最终调好；这些需要后续非快进长跑和多职业样本。

## 历史四图基线（v0.1.3）

在本地 HTTP 服务 `http://127.0.0.1:4174/` 上，使用真实浏览器输入完成 10/10 次四图路线，全部通过。测试覆盖战士、法师、道士三种职业，未通过脚本直接写入游戏内部状态；浏览器快照仅用于读取当前地图、生命、毒层、实体和关卡进度。

## 固定验收条件

每一局都必须按以下顺序完成：

1. 灰烬村外击败 8 只普通怪和裂碑领主；按 `T` 进入雾松林。
2. 雾松林击败 10 只普通怪和森林巨猿；按 `T` 进入黑岩矿坑。
3. 黑岩矿坑击败 10 只普通怪和坑道尸皇；按 `T` 进入赤砂大漠。
4. 赤砂大漠击败 10 只普通怪和沙蝎王；检查最终标题与状态。

普通怪和 Boss 均由 Canvas 点击锁定后使用 `J` 普攻、`1~4` 技能、`R` 一刀时刻、`F` 拾取和 `Q` 药水。Boss 战生命不足或毒层达到阈值时，驱动点击当前地图安全点，推进 1.8 秒恢复，再返回 Boss 区；这是对玩家真实撤离操作的自动化复现。每张地图通关后，驱动先读取自动存档，再真实刷新页面，确认当前地图与完成状态恢复后才继续下一关。

## 十次结果

| 局次 | 职业 | 最终等级 | 地图完成 | 最终结算 | 结果 |
|---:|---|---:|---:|---|---|
| 1 | 战士 | 16 | 4/4 | 纵向切片已通关 / 全部完成 | PASS |
| 2 | 法师 | 16 | 4/4 | 纵向切片已通关 / 全部完成 | PASS |
| 3 | 道士 | 16 | 4/4 | 纵向切片已通关 / 全部完成 | PASS |
| 4 | 战士 | 16 | 4/4 | 纵向切片已通关 / 全部完成 | PASS |
| 5 | 法师 | 16 | 4/4 | 纵向切片已通关 / 全部完成 | PASS |
| 6 | 道士 | 16 | 4/4 | 纵向切片已通关 / 全部完成 | PASS |
| 7 | 战士 | 16 | 4/4 | 纵向切片已通关 / 全部完成 | PASS |
| 8 | 法师 | 17 | 4/4 | 纵向切片已通关 / 全部完成 | PASS |
| 9 | 道士 | 16 | 4/4 | 纵向切片已通关 / 全部完成 | PASS |
| 10 | 战士 | 16 | 4/4 | 纵向切片已通关 / 全部完成 | PASS |

命令输出以 `RUN 10/10 warrior Lv.16: PASS` 和 `ALL 10/10 JOURNEYS PASSED` 收尾；本轮同时输出了 40 条 `CHECKPOINT <地图> SAVED+RESTORED`，证明十局的每张地图都完成了自动保存与刷新恢复。单次运行随机掉落与受伤量会变化，但十局均在 1400 次战斗循环上限内完成。

## 失败与修复记录

### 失败 1：沙蝎王战反复低血

初版驱动只在近战区域按药水，没有根据毒层撤到水井；战士在 Boss 还剩约 1925/3000 生命时进入低血循环。修复为：水井半径 70 像素免受尾刺和危险区伤害、毒层加速净化并恢复生命；验收驱动在毒层达到 2 层或生命低于 85% 时点击井侧空地撤离。

### 失败 2：远处城镇点击无效

城镇安全点在当前 Canvas 视口外，直接点击得到负屏幕坐标，事件没有到达 Canvas，驱动随后仍站在 Boss 区。修复为：移动到视口外目标时先用方向键分段移动，进入视口后再点击地面；井中心与赤砂蝎刷新点重叠时改点井西侧空地（1940,540）。

### 交互回归：击杀后残留追击点

击败实体后清除 `moveTarget`，避免普攻自动接近留下旧坐标，松开方向键后角色继续跑向已死亡目标。

## 复验边界

- 已验证：桌面视口 1440×900、真实鼠标/键盘事件、三职业、随机掉落、Boss 阶段伤害、死亡回城、地图独立进度、出口锁定和最终结算。
- 未验证：多人服务器权威结算、真实 PK 爆装、网络抖动、移动端触摸长按、音频和真实支付；这些仍属于正式服务器范围。
